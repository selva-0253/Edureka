pipeline {
    agent any
    environment{
	HOSTS = 'ansible/hosts'
	}
	stages{
        stage('Run Ansible Playbook')
	{
	   steps{
            sshagent(credentials: ['ansible-key']) {
		    
                sh 'ansible -i $HOSTS testserver -m ping'
		 }
        }
    }
}
}
